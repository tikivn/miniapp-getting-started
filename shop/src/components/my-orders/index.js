import { moneyFormatter } from '../../utils/common';

Component({
  props: {
    numOrders: {
      waiting: 0,
      processing: 0,
      shipping: 0
    },
  },

  // Life cycle
  didMount() {
  },
});
