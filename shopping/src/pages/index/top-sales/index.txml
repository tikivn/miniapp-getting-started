<view class="products m-medium">
  <block tiki:if="{{loading}}">
    <skeleton tiki:for="{{[1, 2, 3, 4]}}" />
  </block>

  <block tiki:else>
    <product-item tiki:for="{{products}}" product="{{item}}" />
  </block>
</view>

