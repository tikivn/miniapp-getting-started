.tu-list-item-wrapper {
  border-bottom: none;
}

.item-token {
  border: 1px solid #ebebf0;
  padding: 8px 12px;
  border-radius: 4px;
  margin-bottom: 4px;
}

.token-color-value {
  width: 40px;
  height: 40px;
  border: 1px solid var(--gray20);
}
