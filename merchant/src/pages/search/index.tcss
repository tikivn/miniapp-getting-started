.search-bar {
  position: sticky;
  top: 0;
  z-index: 2;
  background: white;
}

.search-sort-button-stick {
  position: sticky;
  top: 48px;
  z-index: 2;
  background: white;
}

.empty-divider {
  margin-left: -16px;
  margin-right: -16px;
}
